<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.project.xf.mapper.XfWorkAdviseCcBindMapper">
    
    <resultMap type="XfWorkAdviseCcBind" id="XfWorkAdviseCcBindResult">
        <result property="id"    column="id"    />
        <result property="customerId"    column="customer_id"    />
        <result property="workId"    column="work_id"    />
    </resultMap>

    <sql id="selectXfWorkAdviseCcBindVo">
        select id, customer_id, work_id from xf_work_advise_cc_bind
    </sql>

    <select id="selectXfWorkAdviseCcBindList" parameterType="XfWorkAdviseCcBind" resultMap="XfWorkAdviseCcBindResult">
        <include refid="selectXfWorkAdviseCcBindVo"/>
        <where>  
            <if test="customerId != null "> and customer_id = #{customerId}</if>
            <if test="workId != null "> and work_id = #{workId}</if>
        </where>
    </select>
    
    <select id="selectXfWorkAdviseCcBindById" parameterType="Long" resultMap="XfWorkAdviseCcBindResult">
        <include refid="selectXfWorkAdviseCcBindVo"/>
        where id = #{id}
    </select>
        
    <insert id="insertXfWorkAdviseCcBind" parameterType="XfWorkAdviseCcBind" useGeneratedKeys="true" keyProperty="id">
        insert into xf_work_advise_cc_bind
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="customerId != null ">customer_id,</if>
            <if test="workId != null ">work_id,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="customerId != null ">#{customerId},</if>
            <if test="workId != null ">#{workId},</if>
         </trim>
    </insert>

    <update id="updateXfWorkAdviseCcBind" parameterType="XfWorkAdviseCcBind">
        update xf_work_advise_cc_bind
        <trim prefix="SET" suffixOverrides=",">
            <if test="customerId != null ">customer_id = #{customerId},</if>
            <if test="workId != null ">work_id = #{workId},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteXfWorkAdviseCcBindById" parameterType="Long">
        delete from xf_work_advise_cc_bind where id = #{id}
    </delete>

    <delete id="deleteXfWorkAdviseCcBindByIds" parameterType="String">
        delete from xf_work_advise_cc_bind where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <insert id="saveBatch">
        insert into xf_work_advise_cc_bind (customer_id, work_id) VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.customerId},#{item.workId})
        </foreach>
    </insert>
    
</mapper>