<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.project.system.mapper.SysUserSupplierMapper">

    <insert id="insert">
        insert into sys_user_supplier
        values (#{sysUserId}, #{supplier})
    </insert>

    <update id="update">
        update sys_user_supplier
        set supplier = #{supplier}
        where sys_user_id = #{sysUserId}
    </update>

    <delete id="deleteByIds">
        delete from sys_user_supplier where sys_user_id in
        <foreach collection="collection" item="item" separator="," open="(" close=")">
            #{item}
        </foreach>
    </delete>

    <select id="selectExists">
        select count(1)
        from sys_user_supplier
        where sys_user_id = #{sysUserId}
    </select>

    <select id="selectById" resultType="java.lang.String">
        select supplier
        from sys_user_supplier
        where sys_user_id = #{sysUserId};
    </select>
</mapper>